/** Production browser configuration used by Angular services after build replacement. */
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080/api',
  authDebugEnabled: false,
};
